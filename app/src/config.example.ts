// Copy this file to config.local.ts (gitignored) and fill in real values.
// Get these from your jsonbin.io account -> API Keys -> X-Master-Key.
// This key can read AND write the bin. It is bundled into the APK; that is
// expected here (there is no login/per-user permission model), but it must
// never be used in /web.
export const JSONBIN_BIN_ID = 'your-bin-id';
export const JSONBIN_WRITE_KEY = 'your-x-master-key';
