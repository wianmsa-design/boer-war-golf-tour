// Copy this file to config.local.js (gitignored) and fill in real values.
// Get these from your jsonbin.io account -> API Keys -> create a read-only
// Access Key scoped to "bins: read" only. Never put the X-Master-Key here.
export const JSONBIN_BIN_ID = 'your-bin-id';
export const JSONBIN_READ_KEY = 'your-read-only-access-key';
